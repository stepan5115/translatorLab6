int missingParen(int a, int b) {
    return a + b;
}

void extra() {
    return 123;
}

/*
Раскомментируй ошибку, какую захочешь
Ошибка: пустой вызов функции с неправильным синтаксисом
call(,);
Return в void методе
extra()
Ошибка: неправильный оператор
int badOperator(int x) {
    return x ** 2;
}
// Ошибка: нет точки с запятой после вызова функции
hello()
// Ошибка: неправильный тип
float wrongType() {
    return 0;
}
*/

call(,);